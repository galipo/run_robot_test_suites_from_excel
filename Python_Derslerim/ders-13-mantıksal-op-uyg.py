#1-Girilen bir sayının 0-100 arasında olup olmadığını kontrol ediniz.
"""a = int(input('enter a number:'))
res = 0<a<100
print(f'the number is in the range:{res}')"""

#2-Girilen bir sa ının ozitif çift sayı olup olmadığını kontrol ediniz.
"""a = int(input('enter a number:'))
res =(False==a%2)
print(f'the number is even:{res}')"""

#Notlar
"""
   C   |     Python
   &   |      and 
   |   |      or
   !   |      not

== ifadesi yerine is kullanabiliriz. örneğin: (x==y) yerine (x is y) yazabiliriz. veya (x!==y) yerine (x is not y) diyebiliriz.

ayni işlemleri stringler için "in" komutuyla kullanabiliriz. örneğin   x=['apple']      print('banana' in x)

del komutuyla eleman silme işlemi yapabiliriz. örneğin del x[2]
"""